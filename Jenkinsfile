pipeline {
    agent any
    stages {
        stage('Nginx Build') {
            steps {
                sh 'whoami'
                sh 'pwd'
                //sh 'docker --build -t nginx'
            }
        }
    }
}
